import '../index.css';
import { useSelector, useDispatch } from "react-redux";

function Home() {

    //const selector = useSelector()
    // let currentUser = (selector(state => state.currentUser) ? selector(state => state.currentUser.username) : "");

    return (
        <div>
            <div className='sideStandardMargin'>
            </div>
        </div>
    )
}

export default Home;